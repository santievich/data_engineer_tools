Для запуска уже все готово. Папки dags, clickhouse_data, logs, plugins - обязательно сначала создать пустыми

В терминале прописать:


```docker-compose up airflow-init```


Далее:

```docker-compose up```
